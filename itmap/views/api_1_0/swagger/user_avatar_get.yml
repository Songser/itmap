user avatar
get user avatar
---
tags:
  - user_avatar
parameters:
- name: uid
  in: path
  type: integer
  description: user id
  required: true
responses:
  201:
    description: avatar文件的16进制内容
    schema:
        type: object
        properties:
          avatar:
            type: string
            title: avatar文件的16进制内容
  400:
    description: 默认错误返回
    schema:
      $ref: '#/definitions/GeneralError'
